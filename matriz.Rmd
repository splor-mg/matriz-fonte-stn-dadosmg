---
title: "Untitled"
output: 
  flexdashboard::flex_dashboard
date: "2024-01-18"
---

```{r setup, include=FALSE}
library(data.table)
library(relatorios)
library(jsonlite)
library(crosstalk)

knitr::opts_chunk$set(echo = TRUE)

sd <- SharedData$new(matriz_receita)
```

# Sidebar {.sidebar}

```{r, echo=FALSE}
filter_select("uo", "Unidade", sd, ~UO_COD)
filter_select("fonte", "Fonte", sd, ~FONTE_COD)
filter_select("receita", "Natureza Receita", sd, ~RECEITA_COD)
```

```{r, echo=FALSE}
## Link in Github repo
  downloadthis::download_this(
    matriz_receita,
    output_name = "mtcars data set",
    output_extension = ".csv",
    button_label = "Download",
    button_type = "success",
    has_icon = TRUE,
    icon = "fa fa-save"
  )
```


# Matriz Receita

```{r, echo=FALSE}
reactable::reactable(sd, searchable = TRUE)
```

# Matriz Despesa

```{r, echo=FALSE}
 |> rmarkdown::paged_table()
```

